apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: node-postgres-app

data:
  PORT: "3000"
  DATABASE: "mydatabase"
  DATABASE_HOST: "postgres"
  DATABASE_PORT: "5432"
  NODE_ENV: "production"